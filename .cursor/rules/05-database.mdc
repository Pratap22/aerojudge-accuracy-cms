---
description: Prisma schema, models, and competition-scoped data rules
globs: "database/**/*.{prisma,ts}"
alwaysApply: false
---

# Database

- Schema: `database/prisma/schema.prisma`
- Seed: `database/prisma/seed/index.ts`
- Client consumed as `@npha/database` (see `server/src/config/prisma.ts`)

## Conventions

- IDs: `cuid()`
- Prefer enums for status/role fields
- Competition-scoped children always include `competitionId` (+ cascade where appropriate)
- Unique public results key: `Competition.publicSlug`
- Indexes on competition, status, slug, common lookup columns

## Core models

`Person` + `CompetitionParticipant` + `CompetitionParticipantRole` · `Organization` + `OrganizationSettings` + `OrganizationMember` · `Competition` + `CompetitionSettings` + `RuleProfile` · `Pilot` (competition snapshot + scoring FK) · `Team` / `TeamMember` · `Round` · `Flight` · `Score` / `ScoreApproval` / `Penalty` · `IndividualRanking` / `TeamRanking` · `PrintHistory` · `AuditLog` · `User` / `RefreshToken` / `CompetitionUser` · `CompetitionOfficial`

## Person identity

- Person is global reusable identity (`aeroJudgeId`).
- User.personId is optional 1:1.
- Pilot/Official hold **competition-time snapshots**; do not rewrite historical results from profile updates.
- Sensitive data (emergency/medical) stays on competition Pilot, not global Person.
- See `docs/architecture/PERSON_IDENTITY.md`.

## Rules

- Migrations only via Prisma (`npm run db:migrate`) — do not hand-edit applied SQL as the source of truth.
- Settings drive scoring overrides; do not hardcode rule knobs only in application code when a settings field exists.
- Publish/visibility flags (`isPublished`, score status) must stay consistent with public API filtering.
- After schema changes: `db:generate` and update seed if demo data breaks.
